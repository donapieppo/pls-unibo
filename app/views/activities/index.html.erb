<h1 class="title">Attività</h1>

<div>
  <% @activities.each do |activity| %>  
    <div class="border-2 my-2 p-4">
      <%= activity.name %>
      <div class="my-6 text-sm text-gray-900">
        <%= activity.description %>  
      </div>
      <div class="text-right">
        <%= link_to 'edit', [:edit, activity] %>
      </div>
    </div>
  <% end %>
</div>
