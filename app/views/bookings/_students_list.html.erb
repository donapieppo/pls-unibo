<div class="font-bold">Studenti</div>
<ul>
  <% @activity.bookings.where(teacher_id: current_user.id).each do |b| %>  
    <li>
      <%= b.name %> <%= b.surname %> (<%= b.email %>) <%= link_to_delete_if_deletable b %>  
    </li>
  <% end %>
</ul>

