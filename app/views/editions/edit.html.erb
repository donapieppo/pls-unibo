<div class="lg:flex">
  <div class="lg:w-2/3">
    <%= render partial: 'form' %>  
  </div>
  <div class="lg:w-1/3">
    <h2>Edizioni</h2>
    <% @edition.events.each do |event| %>  
      <div class="py-2">
        <%= link_to [:edit, event] do %> 
          <%= event.name %> 
        <% end %>
      </div>
    <% end %>
    <%= link_to 'nuovo evento', new_edition_event_path(@edition), class: 'button' %>  
  </div>
  </div>
</div>
