<div class="container my-16">
  <div>
    <p class="my-2">Gentile <%= @user.cn %>,</p>
    <p>sei registrato a questo sito con l'indirizzo di posta <%= @user.email %>.</p>
    <p>
    Sei registrato 
    <% if @user.role %>  
      come <strong class="px-2"><%= t @user.role %></strong>
    <% if @user.school_type %> 
        <%= t(@user.school_type).upcase if @user.school_type %><%= @user.other_string if @user.role == 'other' %>. 
        <% if @user.school_id %>
          <%= @user.school %> (<%= @user.school.municipality %>)
        <% end %>
      <% end %>
    <% else %>
      senza avere specificato il tuo ruolo (docente/studente/altro).<br/> Puoi aggiornare i tuoi dati.
    <% end %>
    </p>
  </div>
  <div class="mt-8">
    <%= link_to 'Modifica i miei dati', myedit_users_path, class: 'button' %>  
  </div>

  <div class="p-2">
    <h2 class="title">Le tue prenotazioni</h2>
    <% @user.bookings.each do |b| %>  
      <div class="my-4 bg-pls-light border-l-4 border-pls p-4">
        <%= link_to b.activity.is_a?(Event) ? b.activity.edition : b.activity do %>
          <%= b.activity %>  
        <% end %>
        <div>
          <%= link_to "[Cancella questa iscrizione]", booking_path(b), method: :delete %>  
        </div>
      </div>
      <% end.empty? and begin  %>
      <div class="info">
        Nessuna sei al momento registrato ad alcuna attività.
      </div>
    <% end %>
    <% Booking.where(teacher_id: @user.id) do |b| %>  
      <li><%= b %></li>
    <% end %>
  </div>
</div>

<%= render 'layouts/privacy' %>  
