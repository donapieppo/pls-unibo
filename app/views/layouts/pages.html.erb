<!DOCTYPE html>
<html lang="it">
  <head>
    <%= render 'layouts/seo' %>  
    <title>PlsUnibo</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "fa.all.min", "application", "actiontext" %>
    <%= javascript_include_tag "application" %>
  </head>

  <body>
    <div style="position: fixed; top: 24px; z-index: -1000; opacity: 0.35; width: 150%" >
      <%= image_tag 'Logo_PLS_bg.png' %>  
    </div>

    <div class="flex flex-col justify-between h-screen">
      <div>
        <%= render partial: 'layouts/page_menu' %>  
        <%= render partial: 'layouts/modal' %>  
      </div>

      <div class="flex-grow">
        <%= render FlashMessage::FlashMessagesComponent.new(flash: flash) %>
        <%= yield %>
      </div>

      <%= render partial: 'layouts/page_footer' %>  
    </div>
  </body>
</html>
