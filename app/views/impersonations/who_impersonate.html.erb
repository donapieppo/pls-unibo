<div class="container">
  <h1 class="title">Impersonate</h1>

  <ul>
    <% @users.each do |user| %>
      <li><%= link_to "#{user.cn_militar} (#{user.email})", impersonate_path(user), method: :post %></li>
    <% end %>
  </ul>
</div>
