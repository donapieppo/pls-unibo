<% @contacts.each do |contact| %>  
  <div class="inline-block cursor-pointer my-2">
    <%= link_to contact.to_s, contact_path(contact, modal: 1), data: { turbo_frame: :modal }, class: 'px-2 border-2 border-pls bg-pls text-gray-50 rounded-xl' %>  
  </div>
<% end %>
